<UserControl x:Class="FileExplorrer.UserControls.Item"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileExplorrer.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="750">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>

        <Image Grid.Column="0" Margin="5" Source="{Binding ImagePath}"/>
        <Label Grid.Column="1" HorizontalAlignment="Left" Margin="50 0 0 0"
               VerticalAlignment="Center" Content="{Binding Name}" FontSize="22"/>
    </Grid>
</UserControl>
